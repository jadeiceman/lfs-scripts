name=mpc
version=1.1.0
source=(https://ftp.gnu.org/gnu/mpc/mpc-1.1.0.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr    \
            --disable-static \
            --docdir=/usr/share/doc/mpc-1.1.0
	make
	make html
	make install
	make install-html
}
