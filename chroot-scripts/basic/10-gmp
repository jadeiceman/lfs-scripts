name=gmp
version=6.1.2
source=(http://ftp.gnu.org/gnu/gmp/gmp-6.1.2.tar.xz)

build() {
	cd $name-$version
	./configure --prefix=/usr    \
            --enable-cxx     \
            --disable-static \
            --docdir=/usr/share/doc/gmp-6.1.2
	make
	make html
	make install
	make install-html
}
