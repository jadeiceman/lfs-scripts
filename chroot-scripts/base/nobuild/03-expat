name=expat
version=2.2.5
source=(http://prdownloads.sourceforge.net/expat/expat-2.2.5.tar.bz2)

build() {
	cd $name-$version
	sed -i 's|usr/bin/env |bin/|' run.sh.in
	./configure --prefix=/usr --disable-static
	make
	make install
}
